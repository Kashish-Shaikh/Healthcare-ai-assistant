# ─────────────────────────────────────────────────────────────────────────────
# Healthcare AI Assistant — Python Dependencies
# Python 3.11+  |  All versions verified compatible with each other
#
# Root conflict fixed:
#   ollama >= 0.4.8 requires pydantic >= 2.9
#   Bumped pydantic to 2.9.2 and pydantic-settings to 2.4.0
# ─────────────────────────────────────────────────────────────────────────────

# ── FastAPI & ASGI ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
httpx==0.27.0

# ── Pydantic & Settings ───────────────────────────────────────────────────────
# pydantic>=2.9 required by ollama>=0.4.8 (langchain-ollama dependency)
pydantic==2.9.2
pydantic-settings==2.4.0

# ── LangChain (pinned compatible stack) ──────────────────────────────────────
langchain==0.3.25
langchain-community==0.3.23
langchain-huggingface==0.1.2
langchain-ollama==0.3.3

# ── Vector Store ──────────────────────────────────────────────────────────────
faiss-cpu==1.8.0

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers==3.0.1
torch==2.3.1
transformers==4.41.2
huggingface-hub==0.23.3

# ── Document Loaders ──────────────────────────────────────────────────────────
pypdf==4.2.0
docx2txt==0.8
unstructured==0.14.6

# ── Frontend ──────────────────────────────────────────────────────────────────
streamlit==1.36.0
requests==2.32.3

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-mock==3.14.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
numpy==1.26.4
